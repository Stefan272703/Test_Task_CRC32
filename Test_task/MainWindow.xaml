<Window x:Class="Test_Task.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test_Task"
        mc:Ignorable="d"
        Title="MainWindow" Height="381" Width="692">
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="288*"/>
            <RowDefinition Height="61*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="FileData" SelectionMode="Extended"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" ItemsSource="{Binding}" Margin="10,10,10,5" Grid.ColumnSpan="4" CellEditEnding="FileData_CellEditEnding" >

            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя файла" Binding="{Binding Path = File_Name, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Контрольная сумма" Binding="{Binding Path = Checksum, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                <DataGridTextColumn Header="Путь файла" Binding="{Binding Path = FilePath, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>
        <Button Content="Добавить" Margin="10,10,10,12" Grid.Row="1" Click="Add_File_Click"/>
        <Button Grid.Column="3" Content="Сохранить" Margin="10,10,10,12" Grid.Row="1" Click="Save_File"/>
        <Button Grid.Column="2" Content="Импорт" Margin="10,10,10,13" Grid.Row="1" Click="Import_File"/>
        <Button Grid.Column="1" Content="Удалить строку(-и)" Margin="10,10,10,12" Grid.Row="1" Click="Delete_Row"/>
    </Grid>
</Window>
